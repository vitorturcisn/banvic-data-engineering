apiVersion: v1
kind: Service
metadata:
  name: banvic-postgres
  namespace: banvic
spec:
  type: ClusterIP
  selector:
    app: banvic-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
